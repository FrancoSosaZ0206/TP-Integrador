----------------------------------------------------------------------------------------------------------------------------
						    Lista de bugs 
----------------------------------------------------------------------------------------------------------------------------

Consignas esenciales del TP(Prioridad de 1ra categoria)

		1. Corregir armar reparto
		Se requiere formar multiples clausulas para asegurar que el chofer y el vehiculo esten disponibles
		Asegurar tanto que existan choferes para elegir, (no que la lista de PERSONAS este vacia)
		Asegurar que el chofer elegido exista 
		Asegurar que el chofer elegido no este en un reparto
		Asegurar que el chofer no haya realizado su reparto diario
		Asegurar que no se asigne un paquete que no tenga el estado deposito

		2. Agregar funciones de validacion y verificacion
		Se debe desarrollar funciones que faciliten el punto 1 (Realizadas en gaston v4.1)
		Entre ellas estan, validacion de cuil unico entre clientes y choferes
		Validacion de patente entre los vehiculos
		Validacion de chofer que no se encuentra en algun reparto
		Validacion de chofer que no haya realizado el reparto diario
		Valdiacion de vehiculo que no se encuentre en otro reparto

		3. Agregar campo de reparto diario a persona
		Para poder emplear tanto el punto 1, como el punto 2, 
		desarrollar sus getters y setters
		Setearlo en true cuando se cierra el reparto, al chofer del mismo
		Agregar las funcionalidades al guardado y apertura de archivos de persona
		Agregar al deposito aquellos paquetes que no hayan sido entregados (distintos de 3)

Realizar el pasaje de las funciones:(Prioridad de 2da categoria)

		- [Aclaracion] Extraer las funciones verificadas [CopiaLista, DetectarCambios, MenuGuardarCambios] en la rama MAIN V3.0, que ya estan solucionados
		e intentar implementarlas en la MAIN V4.0


- MENUS:
	- Funcion menuCargarVehiculo: luego de cargar vehiculos tipo AUTO, cuando se quiere emitir el listado de vehiculos con el nuevo vehiculo, todos los autos aparecen como MOTO.
	- Funcion menuModificarVehiculo: no se puede cambiar al tipo AUTO.

	- Funcion menuArmarReparto: no se puede ver un listado de paquetes para meter al reparto.

	- menus de emitir listados: la opcion "-1. MENU PRINCIPAL" funciona como "0. Volver". *** ESTO FUE SOLUCIONADO EN LA VERSION "MAIN-V3.0.0-FINAL" ***

	- Funcion menuCargarPaquete: cuando se quiere asignar un cliente al paquete por CUIL, no muestra el listado de clientes, de modo que no se tiene forma de saber que cuil ingresar, lo que genera que el usuario quede atrapado en el menu hasta que de con un cuil existente.
